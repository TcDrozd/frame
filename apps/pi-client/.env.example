# Installed to /etc/kiosk/kiosk.env (mode 0600, root:kiosk) — never commit the real file.
KIOSK_TOKEN=your-long-random-secret

# Optional overrides (defaults shown)
# KIOSK_BIND=0.0.0.0
# KIOSK_PORT=5000
# KIOSK_URL_FILE=/var/lib/kiosk/url.txt
